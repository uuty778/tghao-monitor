import requests
import json
import os

print("🚀 开始执行监控任务...")
print(f"📂 当前工作目录: {os.getcwd()}")

# 尝试列出所有文件，看看有没有 orders.json
try:
    files = os.listdir('.')
    print(f"📄 当前目录文件: {files}")
except Exception as e:
    print(f"❌ 列文件失败: {e}")

# 模拟数据（你可以以后换成真实逻辑）
orders = [
    {"id": "GIST_TEST_001", "status": "paid", "amount": 100, "time": "2025-09-08"},
    {"id": "GIST_TEST_002", "status": "shipped", "amount": 200, "time": "2025-09-07"}
]

# 保存
try:
    with open('orders.json', 'w', encoding='utf-8') as f:
        json.dump(orders, f, ensure_ascii=False, indent=2)
    print(f"✅ 成功保存 {len(orders)} 条数据到 orders.json")
except Exception as e:
    print(f"❌ 保存文件失败: {e}")
    raise

print("🎉 任务结束！")
