name: Monitor
on:
  schedule:
    - cron: "0 * * * *"
  push:
    branches: [ main ]

jobs:
  run-monitor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Monitor
        run: echo "Monitor is running..."
